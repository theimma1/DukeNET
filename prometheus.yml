global:
  scrape_interval: 15s
  evaluation_interval: 15s
  external_labels:
    monitor: 'ains-monitor'

scrape_configs:
  - job_name: 'ains'
    static_configs:
      - targets: ['localhost:8000']
    metrics_path: '/metrics'
    scrape_interval: 5s
